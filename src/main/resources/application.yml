spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/micro
    username: postgres
    password: sarra
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
